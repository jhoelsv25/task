import { FilterHourPipe } from './filter-hour.pipe';

describe('FilterHourPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHourPipe();
    expect(pipe).toBeTruthy();
  });
});
